# 大众速腾车型适配可行性文档

## 1. 概述

本文档分析如何为24款大众速腾（无ACC）添加openpilot车型适配，实现车道显示和预测功能（不进行控制）。

## 2. 车型识别机制分析

### 2.1 openpilot车型识别流程

```
CAN指纹采集 → 固件版本查询 → VIN码解析 → 车型匹配 → 参数配置
```

### 2.2 关键识别要素

1. **CAN指纹 (fingerprint.json)**
   - CAN消息ID及数据长度
   - 用于初步车型筛选

2. **固件版本 (FW_VERSIONS)**
   - ECU固件版本号
   - 用于精确车型匹配

3. **VIN码信息**
   - WMI (世界制造商识别码)
   - 底盘代码 (chassis_codes)

## 3. 大众速腾适配方案

### 3.1 数据采集需求

#### 3.1.1 CAN指纹采集
```bash
# 使用现有工具采集CAN数据
python tools/vw/cabana_csv_to_fingerprint.py
```

需要采集的关键CAN消息：
- 转向角度传感器
- 车速信息
- 车道线检测相关
- 仪表盘状态

#### 3.1.2 固件版本采集
```python
# 需要查询的ECU
required_ecus = {
    Ecu.engine: 0x7e0,      # 发动机控制单元
    Ecu.srs: 0x715,         # 安全气囊系统
    Ecu.eps: 0x712,         # 电动助力转向
    Ecu.fwdRadar: 0x757,    # 前向雷达（如有）
}
```

### 3.2 车型定义

#### 3.2.1 在values.py中添加速腾定义
```python
VOLKSWAGEN_SAGITAR_MK8 = VolkswagenMQBPlatformConfig(
    [
        VWCarDocs("Volkswagen Sagitar 2024", "24款大众速腾"),
    ],
    VolkswagenCarSpecs(
        mass=1400,           # 车重(kg) - 需实测
        wheelbase=2.69,      # 轴距(m) - 查规格书
        steerRatio=15.6,     # 转向比 - 需实测或查规格
    ),
    chassis_codes={"XX"},    # 底盘代码 - 从VIN获取
    wmis={WMI.SAIC_VOLKSWAGEN},  # 上汽大众
)
```

#### 3.2.2 在fingerprints.py中添加指纹数据
```python
CAR.VOLKSWAGEN_SAGITAR_MK8: {
    # 从采集的fingerprint.json复制数据
    (Ecu.engine, 0x7e0, None): [
        # 发动机ECU固件版本
    ],
    (Ecu.srs, 0x715, None): [
        # 安全气囊ECU固件版本
    ],
    (Ecu.eps, 0x712, None): [
        # 转向ECU固件版本
    ],
    # 注意：无ACC车型可能没有fwdRadar
}
```

### 3.3 功能配置

#### 3.3.1 仅显示模式配置
```python
# 在interface.py的_get_params方法中添加
if candidate == CAR.VOLKSWAGEN_SAGITAR_MK8:
    # 仅显示模式，不进行控制
    ret.dashcamOnly = True
    ret.openpilotLongitudinalControl = False
    ret.pcmCruise = True
    ret.radarUnavailable = True  # 无雷达

    # 禁用所有控制功能
    ret.steerControlType = structs.CarParams.SteerControlType.none
    ret.lateralTuning.which = 'none'
```

## 4. 实施步骤

### 4.1 数据采集阶段

1. **准备工作**
   ```bash
   # 连接OBD-II接口
   # 启动openpilot调试模式
   export LOGPRINT=debug
   ```

2. **CAN数据采集**
   ```bash
   # 记录CAN消息
   python selfdrive/debug/get_fingerprint.py

   # 生成指纹文件
   python tools/vw/cabana_csv_to_fingerprint.py input.csv > sagitar_fingerprint.json
   ```

3. **固件版本查询**
   ```bash
   # 查询ECU固件版本
   python selfdrive/debug/debug_fw_fingerprinting_offline.py
   ```

### 4.2 代码修改阶段

1. **添加车型定义**
   - 修改 `opendbc/car/volkswagen/values.py`
   - 添加速腾车型配置

2. **添加指纹数据**
   - 修改 `opendbc/car/volkswagen/fingerprints.py`
   - 添加CAN指纹和固件版本

3. **配置车型参数**
   - 修改 `opendbc/car/volkswagen/interface.py`
   - 设置仅显示模式参数

### 4.3 测试验证阶段

1. **编译测试**
   ```bash
   scons -j8
   ```

2. **车型识别测试**
   ```bash
   python -c "from opendbc.car import get_car; print(get_car('fingerprint.json', None, None))"
   ```

3. **功能测试**
   - 验证车道线显示
   - 验证预测轨迹
   - 确认无控制输出

## 5. 技术难点与解决方案

### 5.1 无ACC系统适配

**问题**: 速腾无ACC，缺少前向雷达和相关CAN消息

**解决方案**:
```python
# 在fingerprints.py中不包含fwdRadar
# 在interface.py中设置
ret.radarUnavailable = True
ret.openpilotLongitudinalControl = False
```

### 5.2 CAN网络架构识别

**问题**: 需要确定CAN网络拓扑结构

**解决方案**:
```python
# 通过检测特定消息判断网络位置
if any(msg in fingerprint[1] for msg in (0x40, 0x86, 0xB2, 0xFD)):
    ret.networkLocation = NetworkLocation.gateway
else:
    ret.networkLocation = NetworkLocation.fwdCamera
```

### 5.3 底盘代码获取

**问题**: 需要从VIN码获取准确的底盘代码

**解决方案**:
```python
# VIN码第8-9位为底盘代码
# 例如: LSVXXXXXXXX[XX]XXXXXX
# 需要实车获取完整VIN码
```

## 6. 预期效果

### 6.1 实现功能
- ✅ 车道线检测与显示
- ✅ 行驶轨迹预测
- ✅ 车辆状态监控
- ✅ 基础UI显示

### 6.2 限制功能
- ❌ 纵向控制（ACC）
- ❌ 横向控制（车道保持）
- ❌ 自动紧急制动
- ❌ 盲点监测

## 7. 风险评估

### 7.1 技术风险
- **低风险**: 仅显示功能，无控制输出
- **中风险**: CAN消息解析可能需要调试
- **低风险**: 车型识别失败可回退到通用模式

### 7.2 安全风险
- **极低**: 无任何车辆控制功能
- **建议**: 添加明确的免责声明

## 8. 开发时间估算

| 阶段 | 预估时间 | 说明 |
|------|----------|------|
| 数据采集 | 2-3天 | CAN数据和固件版本采集 |
| 代码开发 | 3-5天 | 添加车型定义和配置 |
| 测试验证 | 2-3天 | 功能测试和调试 |
| 文档整理 | 1天 | 更新相关文档 |
| **总计** | **8-12天** | 包含调试和优化时间 |

## 9. 后续扩展

### 9.1 功能扩展可能性
- 如果后期加装ACC系统，可升级支持纵向控制
- 可考虑支持更多大众系车型
- 可添加更多显示功能（如车距显示等）

### 9.2 维护计划
- 定期更新固件版本数据库
- 跟踪openpilot主线更新
- 收集用户反馈优化显示效果


在汽车技术的分类中，24款速腾（Sagitar）在机械平台和电子架构上属于 MK7（第七代）的深度改款（通常称为 MK7.5），而不是 MK8。
这一区分对于安装 openpilot 非常关键，以下是详细原因：
1. 平台架构：依旧是 MQB，而非 MQB Evo
24款速腾 (MK7.5)：基于 MQB 平台。它使用的电子电气架构与全球版的 Jetta A7 一致。
MK8 (第八代)：通常指代高尔夫 8（Golf 8）或海外的新款帕萨特，它们基于全新的 MQB Evo 平台。
对 openpilot 的影响：MQB 平台的协议非常成熟，openpilot 对其支持极好。而 MQB Evo 平台（如高尔夫 8）通常带有 SFD（车辆诊断保护）安全网关，破解和适配的难度更高。
2. 车载协议：CAN 协议
24款速腾虽然内饰升级了悬浮大屏和部分数字化配置，但其底盘控制系统（转向、制动）依然沿用 MQB 1.0/1.5 的协议。
在 openpilot 的代码库中，你应该选择 MQB 车型 对应的配置。
3. 速腾 24 款安装 openpilot 的关键点
既然它是 MK7 架构，你在操作时需要注意：
确认是否有原厂 ACC：24款速腾的高配版自带自适应巡航，这种车型是“插头即用”的最佳选择。如果是手动后装的 ACC，需要确认雷达型号。
网关位置：速腾的网关（Gateway）通常在驾驶员踏板上方，你需要将 VAG 线束 接入网关位置以获取车辆 CAN 总线数据。
车道保持辅助（LKAS）摄像头：24款速腾挡风玻璃上方通常已经预留了单目摄像头（前置摄像头），openpilot 的线束通常会串聯在这个位置。
4. 2026 年的技术现状
到 2026 年，openpilot 社区对速腾所在的 MQB 平台已经实现了全功能覆盖。
支持：全速域自适应巡航、打灯自动变道。
推荐分支：如前所述，使用 sunnypilot，它在 2026 年的版本中已经完美适配了 24 款速腾的扭矩输出，能够提供比原厂更丝滑的 L2+ 级自动驾驶体验。
总结：24款速腾在 openpilot 体系内被归类为 MQB (MK7) 车型，适配非常成熟，不需要担心 MK8 平台特有的网关锁（SFD）问题。


## 10. 结论

大众速腾车型适配在技术上完全可行，主要工作集中在数据采集和配置文件修改。由于仅实现显示功能，风险极低，开发周期较短。建议按照本文档的步骤逐步实施，预期能够成功实现车道显示和预测功能。
