# openpilot 日志系统层次说明

## 日志输出的三个层次

### 1. 控制台输出 (stdout/stderr)
**位置**: 终端窗口直接显示
**内容**: 
- 进程启动信息
- 实时状态显示 (如进程颜色状态)
- 错误和警告信息
- Python print() 输出

**特点**:
- 不会自动保存到文件
- 只在终端运行时可见
- 包含 ANSI 颜色代码

**示例**:
```
manager start
🟢pandad 🟢ui 🟢logmessaged 🔴modeld
```

### 2. swaglog 系统日志
**位置**: `/data/logs/swaglog.*` 或 `~/.comma/log/swaglog.*`
**内容**:
- cloudlog.info/debug/warning/error 调用
- 结构化日志消息
- 进程间通信日志

**控制**:
```bash
export LOGPRINT=debug  # 控制台也显示这些日志
export LOGPRINT=info   # 只显示 info 及以上级别
```

### 3. 数据记录日志
**位置**: `/data/media/0/realdata/*/rlog.bz2`
**内容**:
- 所有 cereal 消息
- CAN 数据
- 传感器数据
- 系统状态

## 如何捕获控制台日志

### 方法1: 使用 tee 命令
```bash
./op 2>&1 | tee /data/logs/console.log
```

### 方法2: 重定向到文件
```bash
./op > /data/logs/console.log 2>&1
```

### 方法3: 使用 script 命令
```bash
script -c "./op" /data/logs/session.log
```

### 方法4: 修改启动脚本自动记录
```bash
# 在 start_openpilot.sh 中添加
exec ./launch_openpilot.sh 2>&1 | tee /data/logs/console_$(date +%Y%m%d_%H%M%S).log
```

## 当前日志配置检查

让我检查当前的日志配置和输出情况。